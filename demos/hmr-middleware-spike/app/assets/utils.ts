/**
 * Utility functions used by Counter.
 * Edit this file to test import propagation HMR!
 */

export function formatCount(count: number): string {
  return `Count: ${count}`
}

export function getButtonStyle() {
  return {
    padding: '12px 24px',
    fontSize: '18px',
    cursor: 'pointer',
    backgroundColor: '#4a90d9',
    color: 'white',
    border: 'none',
    borderRadius: '4px',
  }
}
